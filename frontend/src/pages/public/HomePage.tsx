import { PageWrapper } from '../../shared/components/layout/PageWrapper'

export default function HomePage() {
  return (
    <PageWrapper
      title="HomePage"
      subtitle="Phase 10 only needs the app shell live right now, so this page is intentionally lightweight and ready for the next build-out."
    />
  )
}
