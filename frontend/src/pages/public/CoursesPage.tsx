import { PageWrapper } from '../../shared/components/layout/PageWrapper'

export default function CoursesPage() {
  return (
    <PageWrapper
      title="CoursesPage"
      subtitle="Course browsing comes later. For now we just need the route, layout, and app state to be ready."
    />
  )
}
