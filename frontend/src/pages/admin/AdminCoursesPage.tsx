import { PageWrapper } from '../../shared/components/layout/PageWrapper'

export default function AdminCoursesPage() {
  return (
    <PageWrapper
      title="AdminCoursesPage"
      subtitle="Course management will plug in after the bootstrap phase; the route is live now."
    />
  )
}
